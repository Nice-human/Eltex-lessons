BIN := client
SRC := $(wildcard *.c)
OBJ := $(SRC:.c=.o)

CC := gcc

$(BIN): $(OBJ)
		# Команда: скомпилировать все объектные файлы ($^) в исполняемый файл ($@)
		$(CC) $^ -o $@ 

%.o: %.c
		# Команда: скомпилировать исходный файл ($<) в объектный файл ($@)
		$(CC) -c $< -o $@

.PHONY: clean

clean:
		rm -rf *.o $(BIN)